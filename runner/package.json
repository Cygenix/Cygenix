{
  "name": "cygenix-runner",
  "version": "1.0.0",
  "description": "Cygenix migration runner — executes scheduled and manual conversion jobs against SQL Server / Azure SQL targets. Runs as an Azure Container Instance, no time cap, drives off the same Cosmos run records the Netlify dashboard polls.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@azure/cosmos": "^4.1.0",
    "mssql": "^11.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
